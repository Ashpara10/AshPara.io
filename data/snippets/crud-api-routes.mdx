---
title: "Creating CRUD Api Routes in Nextjs."
description: "Creating Production ready Api-routes inbuilt in Next js!!"
createdAt: "2022-02-06"
logo: "/twitter_profile.jpg"
---

Nextjs provides an api folder in the `pages` directory

```jsx
import { NextApiHandler, NextApiRequest, NextApiResponse } from "next";

const handler: NextApiHandler = async (
  req: NextApiRequest,
  res: NextApiResponse
) => {
  if (req.method === "PUT") {
    res.status(201).json({});
  }

  if (req.method === "GET") {
    res.status(200).json({});
  }

  if (req.method === "POST") {
    res.status(200).json({});
  }

  if (req.method === "DELETE") {
    res.status(204).json({});
  }
};

export default handler;
```
